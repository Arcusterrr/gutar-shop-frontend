export type User = {
    name: string
    login: string
    password: string
}

export type Product = {
    id : number
    name : string
    cost : number
    img : string
    description : string
    count : number
}